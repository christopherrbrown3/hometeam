import { Button } from '../../components/ui/Button'
export function CompleteDialog({ onConfirm }: Readonly<{ onConfirm: (keepOriginalRotation: boolean) => void }>) { return <section aria-label="Complete task confirmation" className="rounded-panel border border-border p-4"><h2 className="font-bold">Complete this task?</h2><p className="mt-1 text-sm text-muted">This action is shared with your household.</p><div className="mt-3 flex gap-2"><Button onClick={() => onConfirm(false)}>Complete</Button><Button onClick={() => onConfirm(true)} variant="secondary">Keep original rotation</Button></div></section> }
